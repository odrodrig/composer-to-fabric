'use strict';

var simpleAsset = require('./simpleAsset');
var simpleParticipant = require('./simpleParticipant');

module.exports = class SimpleTransaction {

    constructor(owner, newOwner, item) {
        
    }
}